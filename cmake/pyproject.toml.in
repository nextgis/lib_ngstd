[build-system]
requires = ["PyQt-builder >=1.15, <2", "sip >=6, <7"]
build-backend = "sipbuild.api"

[tool.sip.metadata]
name = "ngstd"
version = "@VERSION@"
author = "NextGIS"

[tool.sip.project]
sip-include-dirs = ["@PYQT_SIP_DIR@"]
