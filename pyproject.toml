[project]
name = "ngstd-widgets-tooling"
version = "1.0.0"
requires-python = ">=3.8"

[tool.ruff]
target-version = "py38"
line-length = 79
extend-exclude = [
    "build",
    "out",
]

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]

[tool.ruff.format]
quote-style = "double"
